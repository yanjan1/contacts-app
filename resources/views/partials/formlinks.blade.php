<div class="my-4">
    <hr class="color-muted">
    <p>Forgot password? <a href="{{ route('password.request') }}"> Click here to reset</a> or already have an account ?
        <a href="{{ route('login') }}"> sign in here</a>, dont have an account yet ? <a
            href="{{ route('register') }}">sign
            up </a>here, or have an unverified account then <a href="{{ route('verification.notice') }}">verify</a> email
        here.</p>

</div>
